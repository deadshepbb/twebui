// import { getIndicatorList } from "@/api/common";
const actions = {
  // // 指标选择ajax
  // getIndicatorList({ commit, state }) {
  //   getIndicatorList().then(res => {
  //     commit('setStateVal',{
  //       state:'headData.indicatrixs',
  //       val:res.data
  //     })
  //   })
  // }
}
export default actions
